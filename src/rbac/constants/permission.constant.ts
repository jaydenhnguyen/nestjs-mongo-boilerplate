export enum MODULE_NAME {
  ALL = '*',
  USER = 'user',
}

export enum ACTION {
  ALL = '*',
  CREATE = 'create',
  READ = 'read',
  LIST = 'list',
  UPDATE = 'update',
  DELETE = 'delete',
}

export enum SCOPE {
  ALL = '*',
  SELF = 'self',
  EMPLOYEE = 'employee',
}
